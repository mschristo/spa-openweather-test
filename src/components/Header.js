import React from 'react';

class Header extends React.Component {
    render() {
        return (
            <header>
                <h1>OpenWeather</h1>
                <p>Search for a location, to get information about its weather using OpenWeather.com</p>
            </header>
        )
    }
}

export default Header;